# Excalidraw JSON 规范（Smart Illustrator 用）

生成 Excalidraw 图表时必须遵循此规范。

## JSON 顶层结构

```json
{
  "type": "excalidraw",
  "version": 2,
  "source": "https://excalidraw.com",
  "elements": [...],
  "appState": { "gridSize": null, "viewBackgroundColor": "#ffffff" },
  "files": {}
}
```

文件扩展名：`.excalidraw`

## 元素模板

每个元素必须包含以下字段（**禁止**添加 `frameId`、`index`、`versionNonce`、`rawText`）：

```json
{
  "id": "unique-id",
  "type": "rectangle",
  "x": 100, "y": 100,
  "width": 200, "height": 50,
  "angle": 0,
  "strokeColor": "#1e1e1e",
  "backgroundColor": "transparent",
  "fillStyle": "solid",
  "strokeWidth": 2,
  "strokeStyle": "solid",
  "roughness": 1,
  "opacity": 100,
  "groupIds": [],
  "roundness": { "type": 3 },
  "seed": 123456789,
  "version": 1,
  "isDeleted": false,
  "boundElements": null,
  "updated": 1,
  "link": null,
  "locked": false
}
```

- `boundElements` 必须为 `null`（不是 `[]`）
- `updated` 必须为 `1`（不是时间戳）
- `strokeStyle`：`"solid"`（实线，默认）| `"dashed"`（虚线）| `"dotted"`（点线）。虚线适合表示可选路径、异步流、弱关联等

### Text 元素额外属性

```json
{
  "text": "显示文本",
  "fontSize": 20,
  "fontFamily": 2,
  "textAlign": "center",
  "verticalAlign": "middle",
  "containerId": null,
  "originalText": "显示文本",
  "autoResize": true,
  "lineHeight": 1.25
}
```

### Arrow 元素

箭头需要额外的 `points` 数组和端点绑定：

```json
{
  "type": "arrow",
  "points": [[0, 0], [200, 0]],
  "startBinding": null,
  "endBinding": null,
  "startArrowhead": null,
  "endArrowhead": "arrow"
}
```

## 设计规则

### 文字
- **所有文本必须使用** `fontFamily: 2`（Normal 正常字体）
- 双引号 `"` → `『』`，圆括号 `()` → `「」`
- 字号下限：标题 20-28px，副标题 18-20px，正文 16-18px，注释 14px，**绝对禁止 < 14px**
- `lineHeight: 1.25`
- **禁止 Emoji**

### 文字居中估算

独立 text 元素的 `x` 是左边缘，需手动计算：
- 英文：`estimatedWidth = text.length * fontSize * 0.5`
- 中文：`estimatedWidth = text.length * fontSize * 1.0`
- 居中：`x = centerX - estimatedWidth / 2`

### 布局
- 画布范围：0-1200 x 0-800
- 最小形状尺寸：带文字的矩形/椭圆 ≥ 120x60px
- 元素间距：≥ 20-30px
- 四周留白：50-80px padding

## 风格选择

**默认使用「专业蓝」风格**（与品牌色板一致），除非文章内容明确适合其他风格。保持系列文章的视觉一致性优先于每次配色不同。

| 风格 | 特征 | 适合场景 |
|------|------|---------|
| **专业蓝**（默认） | 深靛蓝、科技蓝、暖琥珀，专业克制 | IT 管理汇报、技术方案、项目规划、企业文档 |
| **量化金融** | 智能蓝、涨红跌绿、米白背景 | 量化交易文章、策略流程、指标图解 |
| **水墨淡雅** | 青灰、黛蓝、藕粉，留白多 | 偏文化感的内容、管理层正式汇报 |
| **极简禅意** | 黑白灰 + 单一强调色 | 概念图、决策树、核心流程 |

---

## 色板体系

### 🎨 风格一：专业蓝（默认，与品牌色板对齐）

**文字颜色（strokeColor）**
| 用途 | 色值 | 名称 |
|------|------|------|
| 标题 | `#1E3A5F` | 深靛蓝 |
| 副标题 | `#334155` | 暗石板 |
| 正文 | `#475569` | 石板灰 |
| 强调 | `#D97706` | 暖琥珀 |

**形状填充色（backgroundColor）**
| 色值 | 名称 | 语义 |
|------|------|------|
| `#DBEAFE` | 浅蓝 | 主要节点、服务、API |
| `#D1FAE5` | 浅绿 | 成功、完成、数据流 |
| `#FED7AA` | 浅琥珀 | 警告、里程碑、待处理 |
| `#E0E7FF` | 浅靛 | 处理中、逻辑层 |
| `#FECACA` | 浅红 | 错误、风险、阻断 |
| `#FEF3C7` | 浅黄 | 备注、说明、辅助 |
| `#F1F5F9` | 浅灰 | 分组背景、外部系统 |

**区域背景（opacity: 25-35）**
| 色值 | 名称 | 语义 |
|------|------|------|
| `#EFF6FF` | 极浅蓝 | 业务层 |
| `#F0FDF4` | 极浅绿 | 数据层 |
| `#F8FAFC` | 极浅灰 | 基础设施层 |

---

### 🎨 风格二：量化金融（量化交易文章专用）

**文字颜色**
| 用途 | 色值 | 名称 |
|------|------|------|
| 标题 | `#1E3A5F` | 深墨蓝 |
| 副标题 | `#334155` | 暗石板 |
| 正文 | `#4A5568` | 石板灰 |
| 涨/做多 | `#DC2626` | 涨红 |
| 跌/做空 | `#059669` | 跌绿 |
| 警示 | `#D97706` | 琥珀橙 |

**形状填充色**
| 色值 | 名称 | 语义 |
|------|------|------|
| `#DBEAFE` | 浅蓝 | 策略模块、核心逻辑 |
| `#FECACA` | 浅红 | 做多信号、上涨相关 |
| `#D1FAE5` | 浅绿 | 做空信号、下跌相关 |
| `#FED7AA` | 浅琥珀 | 风控、警示、风险 |
| `#E0E7FF` | 浅靛 | 数据层、输入 |
| `#FEF3C7` | 浅黄 | 备注、参数配置 |

---

### 🎨 风格三：水墨淡雅（东方韵味，正式汇报）

**文字颜色（strokeColor）**
| 用途 | 色值 | 名称 |
|------|------|------|
| 标题 | `#1a365d` | 黛蓝 |
| 副标题 | `#2c5282` | 深青 |
| 正文 | `#374151` | 墨灰 |
| 强调 | `#c05621` | 赭石 |

**形状填充色（backgroundColor）**
| 色值 | 名称 | 语义 |
|------|------|------|
| `#bee3f8` | 天青 | 输入、起点、数据源 |
| `#c6f6d5` | 竹青 | 成功、完成、正向 |
| `#feebc8` | 杏黄 | 警告、待处理、中转 |
| `#e9d8fd` | 藕粉 | 处理中、特殊 |
| `#fed7d7` | 桃夭 | 错误、关键、阻断 |
| `#fefcbf` | 鹅黄 | 备注、说明、辅助 |
| `#b2f5ea` | 春水 | 存储、缓存、数据层 |

**区域背景（opacity: 25-35）**
| 色值 | 名称 | 语义 |
|------|------|------|
| `#ebf8ff` | 月白 | 展示层、前端 |
| `#faf5ff` | 雪青 | 业务层、逻辑 |
| `#f0fff4` | 艾绿 | 数据层、基础 |

---

### 🎨 风格四：极简禅意（黑白灰 + 单一强调色）

**文字颜色**
| 用途 | 色值 |
|------|------|
| 标题 | `#111827` |
| 副标题 | `#374151` |
| 正文 | `#6B7280` |
| 强调 | 根据内容选一个：科技蓝 `#2563EB` / 暖琥珀 `#D97706` / 成功绿 `#059669` |

**形状填充色**
| 色值 | 语义 |
|------|------|
| `#F3F4F6` | 主要节点（浅灰） |
| `#E5E7EB` | 次要节点（中灰） |
| 强调色 20% opacity | 关键节点 |

---

## 风格选择逻辑

1. 根据 SKILL.md 自动判断的 `--style` 确定 Excalidraw 风格：
   - `business` / `technical` → **专业蓝**
   - `trading-tutorial` → **量化金融**
   - `light` → **专业蓝**（默认）
   - `minimal` → **极简禅意**
2. 同一篇文章内多张 Excalidraw 图使用**同一风格**，保持视觉一致
3. 系列文章保持**同一风格**

---

## 设计原则

1. **品牌一致**：默认使用与品牌色板对齐的配色，保持系列文章视觉统一
2. **语义一致**：同一图表中，相同语义的元素使用相同颜色
3. **对比优先**：浅底深字，深底浅字，保证可读性
4. **留白呼吸**：不要填满每个角落，留白是设计的一部分

### 对比度规则
- 白底文字最浅不低于 `#6b7280`
- 浅色填充上用深色变体文字（如浅绿底 `#D1FAE5` 配深绿字 `#065F46`）
- 避免浅灰文字（`#b0b0b0`、`#9ca3af`）出现在白底上

## Common Mistakes to Avoid

- **文字偏移** — text 的 `x` 是左边缘不是中心，必须用居中公式
- **元素重叠** — 放置前检查与周围元素 ≥ 20px 间距
- **画布留白不足** — 四周留 50-80px padding
- **标题没有居中** — 标题应居中于下方图表整体宽度
- **箭头标签溢出** — 长文字标签超出短箭头，保持标签简短或加大箭头
- **对比度不够** — 文字色不低于 `#757575`
- **字号太小** — 正文最小 16px，绝对禁止 < 14px
